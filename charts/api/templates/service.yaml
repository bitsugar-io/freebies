apiVersion: v1
kind: Service
metadata:
  name: {{ include "freebie-api.fullname" . }}
  labels:
    {{- include "freebie-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freebie-api.selectorLabels" . | nindent 4 }}
