apiVersion: v1
kind: Secret
metadata:
  name: {{ include "freebie-api.fullname" . }}
  labels:
    {{- include "freebie-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  worker-secret: {{ .Values.workerSecret | quote }}
