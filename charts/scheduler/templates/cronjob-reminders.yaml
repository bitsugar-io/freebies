apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "freebie-scheduler.fullname" . }}-send-reminders
  labels:
    {{- include "freebie-scheduler.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.sendReminders.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            {{- include "freebie-scheduler.labels" . | nindent 12 }}
            app.kubernetes.io/component: send-reminders
        spec:
          restartPolicy: OnFailure
          containers:
            - name: reminder
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command:
                - ./scheduler
                - send-reminders
              env:
                - name: SCHEDULER_API_URL
                  value: "http://{{ .Values.api.serviceName }}:{{ .Values.api.port }}"
                - name: SCHEDULER_WORKER_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "freebie-scheduler.fullname" . }}
                      key: worker-secret
