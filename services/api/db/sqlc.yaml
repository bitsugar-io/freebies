version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "../internal/db/migrations/"
    gen:
      go:
        package: "db"
        out: "../internal/db"
        emit_json_tags: true
        emit_empty_slices: true
