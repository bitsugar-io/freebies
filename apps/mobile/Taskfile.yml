version: '3'

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  install:
    desc: Install dependencies
    cmds:
      - bun install

  serve:
    desc: Start Expo development server
    cmds:
      - bunx expo start --go

  ios:
    desc: Start on iOS simulator
    cmds:
      - bunx expo start --ios

  android:
    desc: Start on Android emulator
    cmds:
      - bunx expo start --android

  web:
    desc: Start web version
    cmds:
      - bunx expo start --web

  typecheck:
    desc: Run TypeScript type checking
    cmds:
      - bunx tsc --noEmit

  lint:
    desc: Run ESLint
    cmds:
      - bunx eslint . --ext .ts,.tsx

  clean:
    desc: Clean build artifacts and caches
    cmds:
      - rm -rf node_modules/.cache
      - rm -rf .expo

  reset:
    desc: Full reset (clean + reinstall)
    cmds:
      - rm -rf node_modules
      - rm -rf .expo
      - bun install

  build:ios:
    desc: Build iOS development client (EAS)
    cmds:
      - bunx eas build --platform ios --profile development

  build:android:
    desc: Build Android development client (EAS)
    cmds:
      - bunx eas build --platform android --profile development
